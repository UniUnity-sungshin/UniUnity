"use strict";

const { resolve } = require("path");
const { reject } = require("underscore");
const db = require("../config/db");

class PartnerStorage{
    static async getPartnerStore(universityName){ 
        return new Promise()
    }
};

module.exports = PartnerStorage;